package com.example.onlinemusicstreamapp.database.other

enum class State {
    STATE_CREATED,
    STATE_READY,
    STATE_INITIALIZED,
    STATE_INITIALIZING,
    STATE_PAUSE,
    STATE_ERROR,
    STATE_ENDED

}